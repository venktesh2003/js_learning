// for loops

for( index = 1 ; index<10;index++)
{
    console.log(index);
    
}

// keywords in loops

// 1) break 2)continue

for(i = 1 ; i<=20;i++)
{
    
    console.log( `${i}`);
    if(i === 5) break;
    
}

for(i = 1 ; i<=20;i++)
{
    
    
    if(i ==5) continue;
    console.log( `${i}`);
    
}
